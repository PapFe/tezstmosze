
name: Threeunits

on:
 push

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v2
   - run: g++  *.cpp 
   - run: ./run_test.sh
   - run: diff native_output.txt units/out_ok.txt
