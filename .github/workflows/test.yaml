
name: Threeunits

on:
 push

jobs:
 build:
  runs-on: windows-latest
  steps:
   - uses: actions/checkout@v2
   - run: g++  *.cpp -o main
   - run: ./run_test.sh
   - run: diff out_res.txt units/out_ok.txt
